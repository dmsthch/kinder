<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.cafe24.dmsthch.Teacher.TeacherMapper">

<insert id="insert" parameterType="com.cafe24.dmsthch.Teacher.Teacher">
INSERT INTO teacher 
	(
	teacher_no
	,license_kindergarten
	,teacher_level
	,teacher_name
	,teacher_id
	,teacher_pw
	,teacher_add_day
	,teacher_phone
	)
VALUES 
	(#{teacher_no}
	,"GUEST"
	,#{teacher_level}
	,#{teacher_name}
	,#{teacher_id}
	,#{teacher_pw}
	,#{teacher_add_day}
	,#{teacher_phone}
)
</insert>

<select id="login" parameterType="com.cafe24.dmsthch.Teacher.Teacher" resultType="com.cafe24.dmsthch.Teacher.Teacher">

SELECT 
	teacher_no,teacher_name,teacher_id,teacher_level,license_kindergarten
	FROM 
	teacher
	WHERE
	teacher_id = #{teacher_id}
	AND 
	teacher_pw = #{teacher_pw}
</select>

<!-- teacher_no는 int타입이니  parameterType="int"가 되겠고 반환값은 여러개니까 Teacher전역변수가 된다-->
<select id="OneSelectTeacher" parameterType="int" resultType="com.cafe24.dmsthch.Teacher.Teacher">

SELECT * 
FROM teacher 
WHERE 
teacher_no = #{teacher_no}

</select>

<!-- 회원 아이디 중복 체크 -->
 <select id="sign_up_id_check" parameterType="java.lang.String" resultType="int">
 SELECT
 count(*)
 FROM teacher
 WHERE teacher_id = #{teacher_id}
 </select>

<!-- 전체 교원 조회 table list에 필요 -->
<!--입력시 요구하는 타입이 teacher_no 이기때문에 int이고 여러값을 result하기 때문에 result는 Teacher이다-->
<select id="tableList" parameterType="int" resultType="com.cafe24.dmsthch.Teacher.Teacher">

select 
  teacher_no
, teacher_id
, teacher_level
, teacher_name
, teacher_add_day
, teacher_phone
, teacher_bank
, teacher_account
, teacher_paystep
 from 
 	teacher
 where 
 	teacher_no;

</select>

<!-- 전체 선생님 정보 조회 반 배정시 필요--> <!-- ★ SOT ★ = Select All Teacher -->
<!-- WHERE절의 값이 level의 선생님이다 그러므로 parameterType은 String형이 되고 그에 따른 결과는 여러개니까 Teacher가 된다.-->
<select id="SAT" parameterType="java.lang.String" resultType="com.cafe24.dmsthch.Teacher.Teacher">
SELECT 
  teacher_no
, license_kindergarten
, teacher_name
, teacher_level
FROM
  teacher 
WHERE
  teacher_level = "선생님";
</select>

<!-- 전체 반 정보 조회 --> <!-- ★SAC★ Select All Class -->
<select id="SAC" parameterType="int" resultType="com.cafe24.dmsthch.Child.ChildClass">

SELECT
  class_no
, class_name
, class_year
, class_age
FROM
  kid_class
WHERE
  class_no;

</select>

<!-- 입력값과 결과값을 생각하고 타입을 지정하자. -->  

<!-- 회원탈퇴 후 삭제된 아이디에 들어갈 쿼리 -->
<delete id="withdrawal" parameterType="java.lang.String">
DELETE FROM teacher 
WHERE teacher_id = ${teacher_id};
</delete>

<insert id="removeId" parameterType="java.lang.String">
INSERT INTO remove_id(teacher_id) 
VALUES(${teacher_id});
</insert>
<!-- 회원탈퇴 후 삭제된 아이디에 들어갈 쿼리 END-->

</mapper>